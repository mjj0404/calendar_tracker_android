<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.CalendarTracker" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="android:windowAnimationStyle">@style/WindowAnimationTransition</item>
    </style>

    <style name="WindowAnimationTransition">
        <item name="android:windowEnterAnimation">@android:anim/fade_in</item>
        <item name="android:windowExitAnimation">@android:anim/fade_out</item>
    </style>

    <style name="Theme.CalendarTracker.SplashScreen" parent="Theme.SplashScreen.IconBackground">
        <item name="colorPrimary">@color/white</item>
        <item name="windowSplashScreenBackground">@color/purple_150</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_animated_calendar_check</item>
        <item name="windowSplashScreenAnimationDuration">1500</item>
        <item name="windowSplashScreenIconBackgroundColor">@color/purple_500</item>
        <item name="postSplashScreenTheme">@style/Theme.CalendarTracker</item>
        <item name="android:windowSplashScreenBrandingImage" tools:targetApi="s">@drawable/ic_bitjam_logo</item>
    </style>
</resources>